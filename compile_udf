gcc $(mysql_config --cflags) -shared -fPIC -o get_sequence.so get_sequence.c
cp get_sequence.so $(mysql_config --plugindir)


gcc $(mysql_config --cflags) -shared -fPIC -o get_uuid.so get_uuid.c
cp get_uuid.so $(mysql_config --plugindir)
